# Client portal — copy to .env (not committed)
# Must point at your VizoDesk API (include /api if applicable)
VITE_API_URL=http://localhost:3001/api

# Production (Render static site env), e.g.
# VITE_API_URL=https://vizodesk-api.onrender.com/api
